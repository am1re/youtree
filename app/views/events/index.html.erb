<p style="color: green"><%= notice %></p>

<h1>Events</h1>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", tree_event_path(@tree, event) %>
    </p>
  <% end %>
</div>

<%= link_to "New event", new_tree_event_path %>
